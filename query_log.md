```sql
UPDATE ServeTimesDB SET 
        server=John Doe, 
        seconds_before_next_point=
        40,
        day=2023-09-05, opponent=Jane Doe, 
        game_score=30-40, 
        sets=1, 
        game=0-0 
        WHERE id=1;
```

```sql
DELETE FROM ServeTimesDB WHERE id=1;
```

```sql

        INSERT INTO ServeTimesDB VALUES (
            John Doe, 
            40, 
            2023-09-05, 
            Jane Doe, 
            30-40, 
            1, 
            0-0);
        
```

```sql
SELECT * FROM matches
```

```sql
UPDATE ServeTimesDB SET 
        server=John Doe, 
        seconds_before_next_point=
        40,
        day=2023-09-05, opponent=Jane Doe, 
        game_score=30-40, 
        sets=1, 
        game=0-0 
        WHERE id=1;
```

```sql
DELETE FROM ServeTimesDB WHERE id=1;
```

```sql

        INSERT INTO ServeTimesDB VALUES (
            John Doe, 
            40, 
            2023-09-05, 
            Jane Doe, 
            30-40, 
            1, 
            0-0);
        
```

```sql
SELECT * FROM matches
```

